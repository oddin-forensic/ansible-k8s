---
- name: "Copiando token"
  copy:
    src: "{{ playbook_dir }}/{{ token_file }}"
    dest: /home/join_token
  become: true

- name: "Adicionado maquina ao no master"
  shell: |
    kubeadm reset -f
    cat /home/join_token | tail -2 > out.sh
    sh out.sh
  become: true
